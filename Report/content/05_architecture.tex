% 05_architecture.tex
\section{Architecture}

% Required TikZ libraries for fit/background scopes used in this chapter's diagrams.
\usetikzlibrary{fit,backgrounds, calc, shapes.multipart, shapes.geometric, arrows.meta, positioning}

\subsection{Architectural Overview: MVC Intuition}

The application strictly adheres to the Model-View-Controller (MVC) design pattern. This architectural choice separates the internal representations of data structures, the logic that manipulates them, and the graphical interfaces presented to the user. This separation of concerns allows for a highly modular, maintainable, and extensible codebase.

The architecture is divided into three interconnected categories of components:
\begin{itemize}
    \item \textbf{Controller}: Orchestrates the application. The central \texttt{AppEngine} class serves as the primary coordinator, processing user inputs and coordinating changes across the system. It delegates playback controls to the \texttt{Playback} controller, which manages the flow of time for step-by-step animations.
    \item \textbf{Model}: Represents the mathematical data structures, their algorithms, and their visual states over time. The \texttt{IVisualizable} interface acts as the base abstraction for all concrete implementations (such as Linked Lists, Graphs, and Heaps). When an algorithm is executed, the structure generates a \texttt{Timeline} composed of discrete \texttt{Frame}s. Each frame captures the exact state of the structure at a given execution step.
    \item \textbf{View}: Responsible for rendering the visual representation of the \texttt{Model}'s state. The \texttt{UIManager} handles modern graphical user interface elements via Dear ImGui, providing interactive menus and code panels. The \texttt{Renderer} utilizes SFML to draw graphical shapes based on the data contained in the current \texttt{Frame}.
\end{itemize}

\begin{figure}[h!]
    \centering
    \resizebox{\textwidth}{!}{
\begin{tikzpicture}[
    % Global Styles
    base/.style={rectangle, rounded corners, draw=black, very thick, minimum width=3.2cm, minimum height=1.2cm, align=center, font=\sffamily\small},
    controller/.style={base, draw=green!70!black, fill=green!10},
    view/.style={base, draw=red!70!black, fill=red!10},
    model/.style={base, draw=blue!70!black, fill=blue!10},
    utility/.style={base, draw=orange!80!black, fill=yellow!15},
    layer box/.style={rectangle, draw=#1, dashed, thick, inner sep=15pt, fill=#1!5, rounded corners=10pt},
    % Arrow Styles
    arrow/.style={-{Stealth[scale=1.2]}, thick},
    bidir/.style={<->, >=Stealth, thick},
    inherit/.style={-{Triangle[open,fill=white,scale=1.2]}, thick}
]

% ================= Nodes =================
% Utilities (Bottom Center) - Moved down
\node[utility] (Utilities) at (0, -7.5) {\textbf{Utilities}\\(Pseudocode, Math, Configs)};

% Controller Layer (Top Center) - Kept at top
\node[controller] (AppEngine) at (0, 0) {\textbf{AppEngine}\\(Coordinator)};
\node[controller] (DataManager) at (-5, 0) {\textbf{DataManager}\\(File I/O)};
\node[controller] (Playback) at (5, 0) {\textbf{Playback}\\(Time Controls)};

\begin{scope}[on background layer]
    \node[layer box=green!60!black, fit=(DataManager) (AppEngine) (Playback), label={[font=\sffamily\bfseries\color{green!60!black}]above:Controller Layer}] (ControllerLayer) {};
\end{scope}

% View Layer (Bottom Left) - Moved down
\node[view] (UIManager) at (-5, -6) {\textbf{UIManager}\\(ImGui)};
\node[view] (Renderer) at (-5, -9) {\textbf{Renderer}\\(SFML)};

\begin{scope}[on background layer]
    \node[layer box=red!60!black, fit=(UIManager) (Renderer), label={[font=\sffamily\bfseries\color{red!60!black}]above:View Layer}] (ViewLayer) {};
\end{scope}

% Model Layer (Bottom Right) - Moved down
\node[model] (Timeline) at (3.5, -6) {\textbf{Timeline \& Frames}};
\node[model] (DataStructures) at (8, -3.5) {\textbf{Data Structures}\\(List, Graph, Heap)};
\node[model] (IVisualizable) at (8, -6) {\textbf{IVisualizable}\\(Base Interface)};
\node[model] (IPayload) at (3.5, -9) {\textbf{IPayload}};
\node[model] (IPayloadVisitor) at (8, -9) {\textbf{IPayloadVisitor}};

\begin{scope}[on background layer]
    \node[layer box=blue!60!black, fit=(DataStructures) (IVisualizable) (Timeline) (IPayload) (IPayloadVisitor), label={[font=\sffamily\bfseries\color{blue!60!black}]above:Model Layer}] (ModelLayer) {};
\end{scope}

% ================= Intra-Layer Relationships =================
% Controller
\draw[bidir] (AppEngine) -- (DataManager);
\draw[bidir] (AppEngine) -- (Playback);

% View
\draw[arrow] (UIManager) -- (Renderer);

% Model
\draw[inherit] (DataStructures) -- (IVisualizable);
\draw[arrow] (IVisualizable) -- (Timeline) node[midway, above, font=\scriptsize\sffamily] {generates};
\draw[arrow] (Timeline) -- (IPayload) node[midway, right, font=\scriptsize\sffamily] {owns};
\draw[arrow] (IPayloadVisitor) -- (IPayload) node[midway, above, font=\scriptsize\sffamily] {visits};


% ================= Cross-Layer Connections =================

% --- AppEngine Connections ---
\draw[arrow, color=green!60!black] (AppEngine.south west) to[bend right=20] node[midway, left, xshift=-5pt, font=\scriptsize\sffamily] {updates} (UIManager.north east);
\draw[arrow, color=green!60!black] (AppEngine.south east) to[bend left=20] node[midway, right, xshift=5pt, font=\scriptsize\sffamily] {routes actions} (Timeline.north west);

% --- Playback Connections ---
\draw[arrow, color=green!60!black, dashed] (Playback.south) to[bend left=15] node[midway, right, font=\scriptsize\sffamily] {traverses} (Timeline.north east);

% --- View to Model Connections ---
% Renderer implements IPayloadVisitor
\draw[inherit, color=red!60!black, dashed] (Renderer.south) -- ++(0,-0.8) -| (IPayloadVisitor.south) node[pos=0.25, above, font=\scriptsize\sffamily] {implements};
% Renderer renders IPayload
\draw[arrow, color=red!60!black] (Renderer.east) -- (IPayload.west) node[midway, above, font=\scriptsize\sffamily] {renders};
% UIManager queries state of Timeline
\draw[arrow, color=red!60!black] (UIManager.east) -- (Timeline.west) node[midway, above, font=\scriptsize\sffamily] {queries state};

% --- View to Utility Connections ---
\draw[arrow, color=red!60!black] (UIManager.south east) -- (Utilities.north west) node[midway, below left, font=\scriptsize\sffamily] {reads context};

\end{tikzpicture}
    }
    \caption{Application Architecture Diagram detailing the relationships between MVC components.}
    \label{fig:arch_diagram}
\end{figure}

\subsection{Detailed System Design and Interfaces}

A cornerstone of the application's design is its extensive use of abstract interfaces. This approach enforces loose coupling, allowing the front-end rendering logic to remain agnostic to the internal implementations of the back-end data structures.

\subsubsection{The \texttt{IVisualizable} Abstraction}
Every data structure in the application inherits from the \texttt{IVisualizable} interface. This ensures that the \texttt{AppEngine} can interact with an AVL Tree, a Graph, or a Singly Linked List using a unified API. When a user action is dispatched (e.g., inserting a node), the specific data structure executes its algorithm and generates a \texttt{Timeline}.

\subsubsection{Timeline and Frames}
Instead of immediately drawing the results of an algorithm, the models generate discrete states.
\begin{itemize}
    \item \textbf{Timeline}: A collection of \texttt{Frame} objects, representing the complete history of an algorithm's execution from start to finish.
    \item \textbf{Frame}: Captures a static snapshot of the visualization at a specific moment in time. A \texttt{Frame} holds the highlighted pseudocode line, variable states, and a list of graphical \texttt{IPayload} objects that need to be drawn.
\end{itemize}
This design effectively isolates the algorithmic logic from the real-time rendering loop. It natively supports playback features such as pausing, rewinding, and fast-forwarding, simply by selecting a different \texttt{Frame} within the \texttt{Timeline}.

\begin{figure}[h!]
    \centering
\begin{tikzpicture}[
    % --- Node Styles ---
    startstop/.style={ellipse, draw=black, very thick, fill=green!20, minimum width=3cm, minimum height=1cm, align=center, font=\sffamily\bfseries},
    process/.style={rectangle, rounded corners, draw=black, very thick, fill=blue!10, minimum width=3.5cm, minimum height=1cm, align=center, font=\sffamily},
    action/.style={rectangle, rounded corners, draw=black, very thick, fill=purple!10, minimum width=3.5cm, minimum height=1cm, align=center, font=\sffamily},
    decision/.style={diamond, aspect=2, draw=black, very thick, fill=orange!20, minimum width=4cm, minimum height=1cm, align=center, font=\sffamily},
    % --- Line Styles ---
    arrow/.style={-{Stealth[scale=1.2]}, thick},
    label/.style={font=\sffamily\footnotesize\bfseries, fill=white, inner sep=2pt}
]

% ================= Nodes =================

% Main Flow (Straight Down)
\node[startstop] (Start) at (0, 0) {Main Loop\\(while window.isOpen)};
\node[process] (Input) [below=0.8cm of Start] {Process Input\\(Poll SFML Events)};
\node[process] (Update) [below=0.8cm of Input] {Update System State\\(UI \& Playback)};
\node[decision] (Decision) [below=0.8cm of Update] {Action\\Requested?};
\node[process] (Render) [below=2.5cm of Decision] {Render Current Frame\\(Draw SFML \& ImGui)};

% Branch Flow (To the Right)
\node[action] (Execute) [right=1.5cm of Decision] {Model Executes Action\\\& Generates Timeline};
\node[action] (Assign) [below=0.8cm of Execute] {Assign Timeline\\to Playback};

% ================= Connections =================

% Main Path
\draw[arrow] (Start) -- (Input);
\draw[arrow] (Input) -- (Update);
\draw[arrow] (Update) -- (Decision);

% Decision: No Path
\draw[arrow] (Decision) -- node[label] {No} (Render);

% Decision: Yes Path
\draw[arrow] (Decision) -- node[label] {Yes} (Execute);
\draw[arrow] (Execute) -- (Assign);

% Re-merge Yes Path into Render
% Draws down from Assign, then left into the top of the Render node
\draw[arrow] (Assign.south) |- ($ (Render.north) + (1.5,0.5) $) -- ($ (Render.north) + (1.5,0) $);

% Loop Back to Process Input
% Draws left from Render, up past the decision/update, and right into Input
\draw[arrow] (Render.west) -- ++(-1.5,0) |- (Input.west);

\end{tikzpicture}
    \caption{Application Execution Flow Loop.}
    \label{fig:app_flow}
\end{figure}

\subsection{Visitor Design Pattern for Rendering}

A major challenge in a visualization system is handling the heterogeneous types of data that need to be drawn. A Singly Linked List requires rendering distinct nodes and directional arrows, while an Adjacency Matrix necessitates rendering a grid. If the \texttt{Renderer} were to use a long series of \texttt{if-else} statements to determine the type of an object before drawing it, the code would quickly become unmaintainable. Furthermore, giving the \texttt{Model} the responsibility to render itself would violate the MVC principles.

To solve this problem cleanly, the application employs the \textbf{Visitor Design Pattern}. 

\subsubsection{Pattern Implementation}
The pattern relies on two primary abstractions:
\begin{enumerate}
    \item \textbf{\texttt{IPayloadVisitor}}: An interface declaring a \texttt{visit()} method for every possible concrete payload type (e.g., \texttt{visit(const LinkedListPayload\&)}, \texttt{visit(const GraphPayload\&)}). The \texttt{Renderer} implements this interface.
    \item \textbf{\texttt{IPayload}}: An interface representing a drawing instruction generated by the Model. It requires an \texttt{accept(IPayloadVisitor\& visitor)} method.
\end{enumerate}

When a \texttt{Frame} is rendered, the \texttt{Renderer} iterates through the \texttt{Frame}'s payload list and calls \texttt{payload->accept(*this)}. Through dynamic dispatch, the concrete payload's \texttt{accept} method is invoked. This method then immediately calls \texttt{visitor.visit(*this)}, effectively passing its strongly-typed self back to the \texttt{Renderer}.

\begin{figure}[h!]
    \centering
\begin{tikzpicture}[
    scale=0.8, transform shape,
    % --- UML Node Styles ---
    umlclass/.style={
        rectangle split, 
        rectangle split parts=2, 
        draw, 
        very thick, 
        fill=white, 
        minimum width=4.5cm, 
        align=center, 
        font=\sffamily
    },
    umlinterface/.style={
        umlclass, 
        fill=blue!5
    },
    % --- Arrow Styles ---
    inherit/.style={
        -{Triangle[open, fill=white, scale=1.5]}, 
        very thick
    },
    dependency/.style={
        -{Straight Barb[scale=1.2]}, 
        dashed, 
        very thick
    }
]

% ================= LEFT SIDE (The Visitors) =================

\node[umlinterface] (IPayloadVisitor) at (0, 0) {
    $\langle\langle$Interface$\rangle\rangle$ \\
    \textbf{IPayloadVisitor}
    \nodepart{second}
    \begin{tabular}{l}
        + visit(LinkedListPayload) \\
        + visit(GraphPayload)
    \end{tabular}
};

\node[umlclass, fill=red!5] (Renderer) at (0, -5.25) {
    \textbf{Renderer}
    \nodepart{second}
    \begin{tabular}{l}
        + visit(LinkedListPayload) \\
        + visit(GraphPayload)
    \end{tabular}
};

% ================= RIGHT SIDE (The Visitables) =================

\node[umlinterface] (IPayload) at (9, 0) {
    $\langle\langle$Interface$\rangle\rangle$ \\
    \textbf{IPayload}
    \nodepart{second}
    \begin{tabular}{l}
        + accept(IPayloadVisitor)
    \end{tabular}
};

\node[umlclass, fill=green!5] (LinkedListPayload) at (9, -3.5) {
    \textbf{LinkedListPayload}
    \nodepart{second}
    \begin{tabular}{l}
        + accept(IPayloadVisitor v) \\
        \textit{\{ v.visit(*this) \}}
    \end{tabular}
};

\node[umlclass, fill=green!5] (GraphPayload) at (9, -7) {
    \textbf{GraphPayload}
    \nodepart{second}
    \begin{tabular}{l}
        + accept(IPayloadVisitor v) \\
        \textit{\{ v.visit(*this) \}}
    \end{tabular}
};


% ================= INHERITANCE CONNECTIONS =================

% Renderer inherits from IPayloadVisitor
\draw[inherit] (Renderer.north) -- (IPayloadVisitor.south);

% Both Payloads inherit from IPayload (Routed cleanly to avoid overlapping boxes)
\coordinate (PayloadSplit) at (9, -1.8);
\draw[inherit] (PayloadSplit) -- (IPayload.south);
\draw[very thick] (LinkedListPayload.north) -- (PayloadSplit);

% Route GraphPayload's inheritance line around the right side of LinkedListPayload
\draw[very thick] (GraphPayload.east) -- ++(1.5, 0) |- (PayloadSplit);


% ================= DEPENDENCY CONNECTIONS =================

% Dashed lines pointing "horizontally across" to the Renderer
\draw[dependency] (LinkedListPayload.west) -- node[above, sloped, font=\sffamily\footnotesize] {calls} (Renderer.east);
\draw[dependency] (GraphPayload.west) -- node[above, sloped, font=\sffamily\footnotesize] {calls} (Renderer.east);

\end{tikzpicture}
    \caption{UML Class Diagram illustrating the Visitor Pattern for Payload rendering.}
    \label{fig:visitor_pattern}
\end{figure}

This architecture guarantees that the \texttt{Model} never references SFML or rendering specifics, while the \texttt{Renderer} avoids costly runtime type checks (\texttt{dynamic\_cast}), resulting in safe, robust, and highly decoupled code.

